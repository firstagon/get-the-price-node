const User = require("../models/user");

module.exports = defaultRequest = async (req) => {
    // const newSession = new User(req.session.id);
    // newSession.session();

    // console.log(UserSession.log().then(res => console.log(res)))
    // console.log(req.session);
}